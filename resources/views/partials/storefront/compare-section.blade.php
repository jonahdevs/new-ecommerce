{{-- props: $title, $cols (total column count including sticky label col) --}}
<tr>
    <td colspan="{{ $cols }}"
        class="bg-zinc-200 px-4 py-2.5 text-[11.5px] font-bold tracking-widest text-zinc-600 uppercase">
        {{ $title }}
    </td>
</tr>
